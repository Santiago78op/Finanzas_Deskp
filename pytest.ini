[pytest]
testpaths = tests
# La raíz del repo entra al sys.path para poder importar app/db/notion_sync
# como módulos sueltos (el proyecto no es un paquete instalable), y tests/
# también para que los helpers de conftest se importen por nombre.
pythonpath = . tests
addopts = -q --strict-markers
